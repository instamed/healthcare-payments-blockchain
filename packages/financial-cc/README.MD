# financial-cc

## Future Improvements

* Make safe id validations (fully qualified names), whether in models constructors or in custom functions
* Make "sub" objects like Account it's own controller for safe validations and scalability