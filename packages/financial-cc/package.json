{
  "name": "financial-cc",
  "version": "0.1.0",
  "description": "Chaincodes package for the Financial module of FHIR",
  "main": "./dist/src/index.js",
  "typings": "./dist/src/index.d.ts",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "clean": "rimraf dist client",
    "tsc": "tsc",
    "build": "npm run clean && npm run client:generate && tsc",
    "prepare": "npm run build",
    "test": "npm run build && mocha --inspect -r ts-node/register tests/*.spec.ts --reporter spec",
    "client:generate": "echo 'Client is not required anymore'"
  },
  "dependencies": {
    "@worldsibu/convector-core-chaincode": "1.2.1-0-alpha.57ce2bb4",
    "@worldsibu/convector-core-controller": "1.2.1-0-alpha.57ce2bb4",
    "@worldsibu/convector-core-model": "1.2.1-0-alpha.57ce2bb4",
    "reflect-metadata": "^0.1.12",
    "yup": "^0.26.6"
  },
  "devDependencies": {
    "@types/yup": "^0.26.11",
    "@types/chai": "^4.1.4",
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.12.5",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "rimraf": "^2.6.2",
    "ts-node": "^8.0.2"
  }
}
